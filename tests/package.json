{
  "name": "chat-like-human-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0"
  }
}
